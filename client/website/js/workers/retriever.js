"use strict";
onmessage = (posts, users, elementsDOM) => {
    //iterates through the element dom (an array of elements) and inserts the
for (const element of elementsDOM) {
let rand = Math.floor(Math.random() * array.element);
let randIndex = posts[rand];
element.querySelector(".body") = elementsDOM.body
element.querySelector(".author").querySelector(".text") = randIndex.author;
element.querySelector(".author").querySelector(".image") = users.randIndex.author.image
 } }
